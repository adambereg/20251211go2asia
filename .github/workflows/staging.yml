name: Deploy Backend to Staging

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy-backend-staging:
    name: Deploy Backend Services to Staging
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: https://staging-api.go2asia.space
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test

      - name: Build backend services
        run: pnpm build --filter='./services/*'

      - name: Deploy backend services to Cloudflare Workers (Staging)
        run: |
          echo "Deploying backend services to Cloudflare Workers staging..."
          # TODO: Add Cloudflare Workers deployment steps when services are ready
          # Example:
          # pnpm --filter='./services/user-service' deploy:staging
          # pnpm --filter='./services/atlas-service' deploy:staging
          # etc.

      - name: Run smoke tests
        run: |
          echo "Running smoke tests against staging API..."
          # TODO: Add smoke test script when available
          # pnpm test:smoke:staging
