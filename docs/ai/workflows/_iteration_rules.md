# Iteration Rules (Правила итераций мультиагентной разработки Go2Asia)

Этот документ описывает, **как должны выполняться повторные циклы разработки**, чтобы избежать хаоса, циклических ошибок и беспорядочных уточнений.

---

# 🤖 Основной принцип
Итерации происходят только в тех случаях, когда результат агента:
- неполный,
- противоречивый,
- содержит ошибки,
- требует доработки на основе выхода следующего этапа.

**Оркестратор управляет всеми итерациями.**

---

# 🔁 Правила итерации по агентам

## 1. Итерации аналитика
Перезапуск допускается, если:
- в ТЗ отсутствуют ключевые требования,
- обнаружены противоречия,
- архитектор запросил уточнения,
- требования изменились после обсуждений.

**Ограничение:** итерации ≤ 3 подряд.

---

## 2. Итерации архитектора
Перезапуск разрешён, если:
- ТЗ изменилось,
- планировщик выявил недостающие части,
- разработчики указали на невозможность реализации.

**Ограничение:** итерации ≤ 2 на один модуль.

---

## 3. Итерации планировщика
Запускаются при:
- изменениях архитектуры,
- изменениях требований,
- значительных изменениях кода.

**Ограничение:** ≤ 2 итерации.

---

## 4. Итерации разработчиков
Фронтенд и бэкенд могут пересобирать решение **столько раз, сколько требуется**, но:

Оркестратор должен:
- требовать отчёт о каждой итерации,
- запрещать переделывать всё с нуля,
- ограничивать до 3 крупных исправлений.

---

## 5. Итерации QA
Запускаются многократно до тех пор, пока:
- все автотесты не проходят,
- основные сценарии работают.

**QA → разработчик → QA** — цикл разрешён.

---

## 6. Итерации SecOps
Допустимые итерации, если:
- найдены критические уязвимости,
- DevOps внёс значительные сетевые изменения.

---

# 🎯 Главная цель правил
**Итерации должны быть осознанными**  
— каждая новая должна решать конкретную проблему, а не перезапускать весь процесс.

Оркестратор обязан фиксировать причину каждой итерации.
