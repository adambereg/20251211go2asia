# Этап 0.4: Чек-лист облачной инфраструктуры

**Цель:** Настроить базовую облачную инфраструктуру для Go2Asia v2.

---

## ✅ Задачи этапа 0.4

### 1. Домен и DNS

- [x] **Домен зарегистрирован**
  - [x] Домен `go2asia.space` зарегистрирован и активен
  - [x] Домен добавлен в Cloudflare (DNS зона создана)

- [x] **DNS записи настроены**
  - [x] `go2asia.space` → CNAME на Netlify (production фронтенд) - проксирован
  - [x] `www.go2asia.space` → CNAME на `go2asia.space` (редирект) - проксирован
  - [x] `api.go2asia.space` → CNAME на API Gateway Worker - проксирован
  - [x] `auth.go2asia.space` → CNAME на Auth Service Worker - проксирован
  - [x] `content.go2asia.space` → CNAME на Content Service Worker - проксирован
  - [x] `token.go2asia.space` → CNAME на Token Service Worker - проксирован
  - [x] `media.go2asia.space` → R2 bucket `go2asia-media` - проксирован
  - [x] Дополнительные записи: `connect`, `chain`, `risk`, `stats`, `rf-api` - проксированы
  - [x] Все записи имеют включённое проксирование (🟧 orange cloud)

### 2. Cloudflare CDN и WAF

- [x] **SSL/TLS настроен** ✅
  - [x] Режим: Full (strict) - включен
  - [x] Always Use HTTPS включено - активно
  - [x] HSTS настроен - включен (6 месяцев, включая поддомены, Preload включен)

- [ ] **WAF включен** (требует платной подписки)
  - [ ] OWASP Core Ruleset включен (требует платную подписку)
  - [ ] Bot Fight Mode включен (требует платную подписку)
  - [ ] Правила для критичных endpoint'ов настроены (требует платную подписку):
    - [ ] `/api/auth/*` - режим блокировки
    - [ ] `/api/payments/*` - режим блокировки
    - [ ] `/api/referral/*` - режим блокировки
  - **Примечание:** На Free плане Managed rules недоступны. Можно использовать Custom Rules (ограниченно)

- [ ] **Rate Limiting настроен** (требует платной подписки)
  - [ ] `/api/auth/login` - 5 запросов/минута
  - [ ] `/api/auth/send-code` - 3 запроса/5 минут
  - [ ] `/api/places/search` - 20 запросов/минута
  - [ ] `/api/events` - 60 запросов/минута
  - **Примечание:** Rate Limiting доступен только на платных планах

- [x] **Кэширование настроено** ✅
  - [x] API запросы (`api.go2asia.space/*`) не кэшируются (Bypass cache) - настроено через Cache Rules
  - [ ] Статические файлы (`/_next/static/*`) кэшируются (TTL: 1 месяц) - настроить при создании фронтенда
  - [x] Правила кэширования применены через Cache Rules

### 3. Cloudflare R2 для медиа

- [x] **R2 Bucket создан/проверен**
  - [x] Bucket `go2asia-media` существует
  - [x] Структура префиксов создана: `country/`, `city/`, `place/`
  - [x] Bucket Size: 8.21 MB (начальная стадия)
  - [x] Public Access: Enabled

- [x] **Публичный доступ настроен**
  - [x] Custom Domain `media.go2asia.space` настроен в R2
  - [x] DNS запись для `media.go2asia.space` добавлена и проксирована
  - [ ] CORS настроен (если нужен доступ из браузера) - проверить при необходимости

- [ ] **Secrets для R2 (если нужны)**
  - [ ] `R2_ACCESS_KEY_ID` добавлен в GitHub Secrets (опционально, только если нужен доступ из CI/CD)
  - [ ] `R2_SECRET_ACCESS_KEY` добавлен в GitHub Secrets (опционально, только если нужен доступ из CI/CD)

### 4. Cloudflare Workers

- [x] **Workers инициализированы (staging)**
  - [x] `go2asia-api-gateway-staging` создан
  - [x] `go2asia-auth-service-staging` создан
  - [x] `go2asia-content-service-staging` создан
  - [x] `go2asia-referral-service-staging` создан
  - [x] `go2asia-token-service-staging` создан
  - [ ] Production Workers созданы (если нужны на этом этапе)

- [x] **Структура сервисов в монорепо подготовлена** ✅
  - [x] `services/api-gateway/` создан
  - [x] `services/auth-service/` создан
  - [x] `services/content-service/` создан
  - [x] `services/referral-service/` создан
  - [x] `services/token-service/` создан
  - [x] Базовые `wrangler.toml` настроены для всех сервисов
  - [x] Минимальные `src/index.ts` созданы с health check endpoints
  - [x] `package.json` и `tsconfig.json` настроены для всех сервисов
  - [x] README.md созданы для всех сервисов

- [x] **Деплой Workers настроен**
  - [x] GitHub Actions workflows обновлены для деплоя Workers
  - [x] Secrets `CLOUDFLARE_API_TOKEN` и `CLOUDFLARE_ACCOUNT_ID` добавлены в GitHub
  - [ ] Тестовый деплой выполнен успешно (после создания структуры в монорепо)

### 5. Документация

- [x] **Документация обновлена**
  - [x] `docs/ops/cloudflare_setup.md` обновлена с доменом `go2asia.space`
  - [x] Раздел про R2 добавлен/обновлен
  - [x] Раздел про инициализацию Workers добавлен
  - [x] `docs/plans/PHASE_0_PROGRESS.md` обновлена с результатами этапа 0.4

---

## 🔍 Проверка работоспособности

После выполнения всех задач проверить:

1. **DNS резолвится корректно:**
   ```bash
   dig go2asia.space
   dig api.go2asia.space
   dig staging-api.go2asia.space
   ```

2. **SSL сертификаты работают:**
   - Открыть `https://go2asia.space` в браузере
   - Проверить, что сертификат валиден
   - Проверить редирект с HTTP на HTTPS

3. **Workers отвечают:**
   - Открыть `https://api.go2asia.space` (должен вернуть базовый ответ)
   - Открыть `https://staging-api.go2asia.space` (должен вернуть базовый ответ)

4. **R2 доступен (если настроен):**
   - Проверить доступ к `https://media.go2asia.space` (должен показать R2 bucket или ошибку 404, но не ошибку DNS)

---

## 📝 Примечания

- Некоторые задачи требуют действий в Cloudflare Dashboard (настройка DNS, WAF, R2)
- Workers можно создать через Wrangler CLI или Cloudflare Dashboard
- Документация должна отражать реальное состояние инфраструктуры

---

**Статус:** ✅ Практически завершён (основная инфраструктура настроена, структура сервисов создана)  
**Дата начала:** 2025-12-12  
**Дата обновления:** 2025-12-12

## 📋 Текущий статус

### ✅ Выполнено:
- Домен `go2asia.space` зарегистрирован и настроен в Cloudflare
- DNS записи настроены для всех сервисов (все проксированы)
- SSL/TLS настроен: Full (strict), Always Use HTTPS, HSTS включен
- Кэширование для API настроено (Bypass cache для `api.go2asia.space`)
- R2 bucket `go2asia-media` создан, публичный доступ включен, Custom Domain настроен
- 5 staging Workers созданы в Cloudflare Dashboard
- Структура сервисов в монорепо создана (`services/*/`)
- Базовые конфигурации (`wrangler.toml`, `package.json`, `tsconfig.json`) настроены
- Минимальные Worker коды с health check endpoints созданы
- GitHub Secrets для Cloudflare добавлены

### ⏳ Опционально (требует платной подписки):
- WAF Managed Rules (OWASP Core Ruleset) - доступны только на платных планах
- Bot Fight Mode - доступен только на платных планах
- Rate Limiting - доступен только на платных планах
- Custom Rules для критичных endpoint'ов - можно настроить на Free плане (ограниченно)

### 🔄 Следующие шаги:
- Выполнить тестовый деплой через GitHub Actions (после настройки routes в Cloudflare)
- Настроить routes для Workers в Cloudflare Dashboard или через `wrangler.toml`
- Добавить переменные окружения (DATABASE_URL и др.) в Cloudflare Workers

