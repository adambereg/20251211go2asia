# Phase 0.7 — Конфигурация деплоя фронтенда и безопасности

**Статус:** ✅ Завершено (подготовка)  
**Дата:** 2025-01-12  
**Этап:** Подготовка к деплою (без фактического деплоя)

---

## 📋 Контекст

**Phase 0.6 завершён:**
- ✅ Боевой фронтенд создан в `apps/go2asia-pwa-shell`
- ✅ Clerk интегрирован и работает
- ✅ Роли проверяются через `sessionClaims.role` в middleware
- ✅ App Shell с базовой навигацией работает
- ✅ PWA функциональность (manifest, service worker) настроена

**Важные ограничения Phase 0.7:**
- ❌ **НЕ выполняется фактический деплой** на Netlify
- ❌ **НЕ подключаются** Netlify/Cloudflare аккаунты
- ❌ **НЕ считается завершённой** фактической публикацией
- ✅ Все действия — конфигурации, инструкции, архитектурные решения, чеклисты

---

## 🎯 Цель Phase 0.7

Подготовить фронтенд к будущему деплою:

1. Определить схему деплоя App Shell и микрофронтендов
2. Подготовить Netlify-конфигурацию (как шаблон)
3. Подготовить Cloudflare-настройки (как рекомендации)
4. Определить и задокументировать политики безопасности (CSP, HSTS)
5. Сформировать чёткий checklist "Как запустить деплой фронтенда"

---

## 📦 Задачи Phase 0.7

### 1. Архитектура деплоя фронтенда ✅

**Результат:** `docs/architecture/frontend_deployment.md`

**Содержание:**
- Общая архитектура деплоя (Cloudflare → Netlify → Backend)
- App Shell как основное приложение
- Микрофронтенды как статические ассеты на CDN
- Версионирование и кеширование
- Структура деплоя (production, staging, preview)
- Переменные окружения
- Build процесс
- Безопасность (Security Headers, CSP, HSTS)
- Мониторинг и логирование
- Rollback стратегия

**Статус:** ✅ Документ создан

---

### 2. Netlify конфигурация (шаблон) ✅

**Результат:** `apps/go2asia-pwa-shell/netlify.toml`

**Содержание:**
- Build settings (команда сборки, publish directory)
- Netlify Next.js plugin
- Environment variables (описание, не секреты)
- Deploy contexts (production, staging, preview, branch-deploy)
- Security headers (базовые)
- Cache headers (для статики, HTML, изображений)
- Redirects (опционально)

**Важно:**
- Это **шаблон** конфигурации
- Реальные секреты настраиваются в Netlify Dashboard
- CSP включена в черновой версии (будет дорабатываться)

**Статус:** ✅ Конфигурация создана

---

### 3. Cloudflare настройки (рекомендации) ✅

**Результат:** Раздел в `docs/ops/cloudflare_setup.md` и рекомендации в этом документе

**Содержание:**

#### 3.1. HTTP Headers (рекомендации)

**Для фронтенда через Cloudflare:**

| Header | Значение | Описание |
|--------|----------|----------|
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains; preload` | HSTS (production only) |
| `X-Content-Type-Options` | `nosniff` | Предотвращает MIME-sniffing |
| `X-Frame-Options` | `DENY` | Предотвращает clickjacking |
| `X-XSS-Protection` | `1; mode=block` | Базовая защита от XSS |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | Контроль referrer |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=(self)` | Ограничение API браузера |

**Настройка:**
- Через Cloudflare Dashboard → Rules → Transform Rules → Modify Response Header
- Или через Cloudflare Workers (если нужна более сложная логика)

#### 3.2. Content Security Policy (CSP)

**Production CSP (черновая версия):**

```
default-src 'self';
script-src 'self' 'unsafe-inline' 'unsafe-eval' https://*.clerk.com https://*.clerk.accounts.dev;
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
font-src 'self' https://fonts.gstatic.com;
img-src 'self' data: https: blob:;
connect-src 'self' https://*.clerk.com https://api.go2asia.space https://*.go2asia.space;
frame-src 'self' https://*.clerk.com;
```

**Важно:**
- CSP включена в `netlify.toml` (может быть переопределена через Cloudflare)
- `'unsafe-inline'` и `'unsafe-eval'` могут быть удалены после оптимизации
- CSP будет дорабатываться после тестирования в staging

**Настройка:**
- Через Cloudflare Dashboard → Rules → Transform Rules → Modify Response Header
- Или через `netlify.toml` (текущий вариант)

#### 3.3. HSTS (HTTP Strict Transport Security)

**Production only:**

```
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
```

**Важно:**
- Включается **только после** полной стабильности конфигурации
- Включается **только для production** (не для staging)
- Настраивается через Cloudflare Dashboard → SSL/TLS → Edge Certificates → HSTS

**Когда включать:**
- После успешного тестирования в staging
- После проверки всех поддоменов
- После финальной проверки HTTPS на всех страницах

#### 3.4. Cache Strategy для JS/CSS/media

**Page Rules / Rulesets в Cloudflare:**

1. **Статические ассеты Next.js:**
   ```
   Pattern: *go2asia.app/_next/static/*
   Cache Level: Cache Everything
   Edge TTL: 1 месяц (2592000 секунд)
   Browser TTL: 1 месяц
   ```

2. **HTML страницы:**
   ```
   Pattern: *go2asia.app/*
   Cache Level: Bypass
   Edge TTL: 0
   Browser TTL: 0
   ```

3. **Изображения:**
   ```
   Pattern: *go2asia.app/images/*
   Cache Level: Cache Everything
   Edge TTL: 1 день (86400 секунд)
   Browser TTL: 1 день
   ```

**Настройка:**
- Через Cloudflare Dashboard → Rules → Page Rules (legacy)
- Или через Cloudflare Dashboard → Rules → Configuration Rules (новый вариант)

**Статус:** ✅ Рекомендации подготовлены

---

### 4. Безопасность

#### 4.1. Что включается в Phase 0.7 (подготовка)

✅ **Готово:**
- Базовые Security Headers (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy)
- Черновая версия CSP (в `netlify.toml`)
- Документация по HSTS (когда и как включать)
- Рекомендации по настройке через Cloudflare

#### 4.2. Что включается только перед production

⏳ **Будет включено перед production:**
- HSTS (через Cloudflare)
- Финальная версия CSP (после тестирования в staging)
- WAF правила для защиты API (если нужно)
- Rate limiting для API endpoints (если нужно)

#### 4.3. Разделение local / staging / production

**Local:**
- Минимальные security headers (для разработки)
- CSP не включена (для удобства разработки)
- HSTS не включена

**Staging:**
- Базовые security headers (как в production)
- CSP включена (черновая версия)
- HSTS **не включена** (для удобства тестирования)

**Production:**
- Все security headers
- Финальная версия CSP
- HSTS включена (после стабилизации)

**Статус:** ✅ Стратегия безопасности задокументирована

---

### 5. Документация ✅

**Созданные документы:**

1. ✅ `docs/architecture/frontend_deployment.md` — архитектура деплоя
2. ✅ `docs/ops/phase0_7_plan.md` — этот документ (план Phase 0.7)
3. ✅ `docs/ops/phase0_7_checklist.md` — чеклист запуска деплоя
4. ✅ `apps/go2asia-pwa-shell/netlify.toml` — шаблон Netlify конфигурации

**Обновлённые документы:**
- `docs/ops/cloudflare_setup.md` — добавлены рекомендации для фронтенда (если нужно)

**Статус:** ✅ Документация создана

---

### 6. Review

#### 6.1. Architecture Review ✅

**Проверено:**
- ✅ Архитектура деплоя соответствует требованиям
- ✅ App Shell деплоится как единое приложение
- ✅ Микрофронтенды интегрированы через Next.js routing
- ✅ Версионирование и кеширование настроены правильно
- ✅ Переменные окружения разделены по окружениям

**Результат:** ✅ Архитектура готова к деплою

#### 6.2. Security Review ✅

**Проверено:**
- ✅ Security Headers настроены корректно
- ✅ CSP включена (черновая версия, будет дорабатываться)
- ✅ HSTS задокументирована (включается перед production)
- ✅ Разделение local/staging/production настроено

**Результат:** ✅ Безопасность подготовлена

#### 6.3. DevOps Readiness Review ✅

**Проверено:**
- ✅ Netlify конфигурация готова (шаблон)
- ✅ Cloudflare рекомендации подготовлены
- ✅ Build процесс задокументирован
- ✅ Rollback стратегия описана
- ✅ Чеклист запуска деплоя создан

**Результат:** ✅ DevOps готовность подтверждена

**Статус:** ✅ Review завершён

---

## ✅ Критерии завершения Phase 0.7

- [x] Архитектурный документ `frontend_deployment.md` создан
- [x] Шаблон `netlify.toml` создан
- [x] Cloudflare рекомендации подготовлены
- [x] Политики безопасности задокументированы
- [x] Чеклист запуска деплоя создан
- [x] Architecture review проведён
- [x] Security review проведён
- [x] DevOps readiness review проведён

---

## 🔄 Связь с Phase 0.8 и далее

**После завершения Phase 0.7:**

- **Phase 0.8+:** Разработка модулей и функциональности
- **Перед production деплоем:** Выполнить чеклист из `phase0_7_checklist.md`
- **Production деплой:** Следовать инструкциям из `phase0_7_checklist.md`

---

## 📚 Связанные документы

- `docs/architecture/frontend_deployment.md` — архитектура деплоя
- `docs/ops/phase0_7_checklist.md` — чеклист запуска деплоя
- `docs/ops/netlify_setup.md` — настройка Netlify
- `docs/ops/cloudflare_setup.md` — настройка Cloudflare
- `docs/ops/environments.md` — описание окружений
- `apps/go2asia-pwa-shell/netlify.toml` — Netlify конфигурация

---

**Дата создания:** 2025-01-12  
**Последнее обновление:** 2025-01-12
